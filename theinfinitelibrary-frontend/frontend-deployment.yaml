apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: europe-north1-docker.pkg.dev/theinfinitelibrary/containers/theinfinitelibrary-frontend:v1
        ports:
        - containerPort: 80

